import { RandomHexDirective } from './random-hex.directive';

describe('RandomHexDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomHexDirective();
    expect(directive).toBeTruthy();
  });
});
